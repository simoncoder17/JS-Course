const colors = ["Red", "Blue", "Green", "Yellow"];

// Destructuring the first two values
const [firstColor, secondColor] = colors;

console.log(firstColor);  // Output: "Red"
console.log(secondColor); // Output: "Blue"
