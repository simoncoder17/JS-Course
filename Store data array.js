// Storing an array of colors
let colors = ["Red", "Green", "Blue", "Yellow"];

// Storing an array of temperatures
let temperatures = [25, 30, 35, 40];

// Accessing elements
console.log(colors[2]);       // Output: Blue
console.log(temperatures[0]); // Output: 25
